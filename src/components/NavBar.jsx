const NavBar = () => {
    return(
        <nav className="w-full bg-primary fixed bottom-0 p-3">
            <ul>
                <li>Inicio</li>
            </ul>
        </nav>
    )
}

export default NavBar;